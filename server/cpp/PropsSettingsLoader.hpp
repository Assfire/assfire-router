#pragma once

#include "SettingsLoader.hpp"

namespace assfire::router {
    class PropsSettingsLoader : public SettingsLoader {
        public:
            void load_settings(Settings& settings) override {
                // [TODO]
            };
    };
}